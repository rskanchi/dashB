git init
git add *
git commit -m "update dashboard using yml for docs"

# use one the these based on whatever the primary branch is called in your repo
git push -u master
or
git push -u main
or 
git push -u origin